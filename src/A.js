var a = (function(){
   return {
       /**
        * @param first
        * @param second
        * @returns {*}
        */
       add:function(first,second){
           return first+second;
       }
   }
})();
